**Memory Summary:**

1. **Daily 6:00 AM Cron Task**: Created a scheduled task to automatically review Aspose.3D reference tutorials each morning. The task pulls latest commits, reviews any changes to tutorial files under `3d/english/(java|net)/`, verifies code snippets against Aspose.3D v26.6.0 using `aspose-cli verify`, fixes any errors, propagates fixes to translations, and commits/pushes progress.

2. **Code Verification Workflow**: 
   - Extract code blocks from `_index.md` files (fenced blocks with `java`, `csharp`, or `C#` info strings)
   - Merge consecutive blocks with matching `ExStart:<tag>`/`ExEnd:<tag>` markers into single verification units
   - Verify using `aspose-cli verify java 26.6.0 <file>` or `aspose-cli verify csharp 26.6.0 <file>`
   - Fix real API errors (unknown classes/methods, wrong signatures) by consulting `aspose-cli api show`/`search`
   - Propagate verified fixes verbatim to all translated language folders

3. **Key Findings from Verified Tutorials** (commit `8565a2f5`):
   - All 7 tutorial files reviewed contained correct code snippets
   - Files verified: `compress-meshes-google-draco`, `xpath-like-object-queries`, `change-plane-orientation`, `compress-3d-scenes`, `get-scene-information`, `managing-3d-properties-scenes`, `save-custom-mesh-formats`
   - All Java snippets compiled successfully with Aspose.3D 26.6.0

4. **Progress Tracking**: The `.claude/progress.md` file tracks the last reviewed commit SHA. After each review session, the progress file is updated and committed to maintain continuity across sessions.

5. **Queue Management**: Commits are processed in oldest-first order. If a commit only modifies the progress file (not tutorial files), it's still recorded as reviewed and the queue advances to the next commit.

6. **Git Workflow**: Always stash uncommitted changes before `git pull --rebase`, then `git stash pop` to restore them. This prevents conflicts when pulling updates from the remote repository.
