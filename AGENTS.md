This repository contains tutorials written in markdown, you will merge all pull requests.

In the pull requests there's only markdown files, which may contain code snippet inside, you will use tool to verify if the code snippet contains invalid API calls then you should correct the code snippets.
